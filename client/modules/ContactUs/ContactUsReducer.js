// Import Actions
// import {  } from './ContactUsActions'

// Initial State
const initialState = {}

const ContactUsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default ContactUsReducer
