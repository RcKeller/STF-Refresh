// Import Actions
// import {  } from './FAQActions'

// Initial State
const initialState = {}

const FAQReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default FAQReducer
