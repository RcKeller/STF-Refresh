// Import Actions
// import {  } from './AboutActions'

// Initial State
const initialState = {}

const AboutReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default AboutReducer
