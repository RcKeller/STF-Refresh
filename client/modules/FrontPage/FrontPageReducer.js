// Import Actions
// import {  } from './FrontPageActions'

// Initial State
const initialState = {}

const FrontPageReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default FrontPageReducer
